# Group 8 Prompt: Schema & Entity SEO

Use `/seo-master schema-audit` to audit provided JSON-LD, HTML, page data, and entity inputs.

Use `/seo-master schema-generate` to generate safe JSON-LD only from provided values.

Never invent reviews, ratings, authors, prices, dates, locations, opening hours, or sameAs URLs. Do not perform live Google Rich Results validation unless future validator support is added.

If no usable input is provided, return `needs_input` and ask for HTML, JSON-LD, page data, organization data, product/service/software/local business/video/job data, or entity information.
