# Getting Started

Install globally:

```bash
npm install -g master-of-seo
```

Run:

```bash
seo-master "/seo-master help"
```

Master of SEO activates only through `/seo-master`, explicit natural activation such as `use seo-master`, or direct MCP tool calls.

