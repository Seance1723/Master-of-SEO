# MCP Setup

```json
{
  "mcpServers": {
    "master-of-seo": {
      "command": "npx",
      "args": ["-y", "master-of-seo", "mcp"]
    }
  }
}
```

Use this with Codex, Antigravity, Windsurf, VS Code, Cursor, Claude, Continue, and other MCP-compatible agents.

